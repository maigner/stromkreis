# Stand: 30. August 2026

Arbeitsstand zum Weiterarbeiten (z.B. am MacBook).

## Neu am 30.8. (abends): Cloud-Zugang auf User-Rolle beschraenkt, SD-Image-Aufraeumen

- **Cloud-Benutzer darf nur noch UI + Aktionen (keine Verwaltung):** Die Cloud-Identitaet (07-openhab-cloud.sh) wird erst gesetzt, nachdem 02b das openHAB-Admin-Konto angelegt hat (`install-gateway.sh`: Schritt cloud jetzt nach wechselrichter; Phasen-Prozente in `gateway-provision.js` angepasst). Grund: solange kein Admin-Konto existiert, serviert openHAB den Einrichtungsassistenten - wer die Anlage ueber hac.stromkreis.net erreicht (Mitglied mit App-Zugang), koennte sich selbst zum Administrator machen. 07 prueft zusaetzlich hart (`die`, wenn kein Administrator existiert) und warnt im klassischen Modus. Mit vorhandenem Admin-Konto gilt die openHAB-Default-Security (implicit user role): unauthentifiziert/via Cloud nur Items lesen, Seiten sehen, Kommandos senden; Admin-Endpoints 401 (auf Pi 71 verifiziert). Achtung Bestand: Pis 50/51 sind online mit Phase `neu` - dort Admin-Konto pruefen bzw. Setup nachziehen.
- **"Für Experten" aus der Main UI entfernt:** Navigationspunkt und Unterseite `stromkreis_experten` in allen 5 Wechselrichter-Profilen (`overview.yaml`) gestrichen; die Items (Entladeleistung, Netzladeschutz, Notfallplan usw.) und die Regel-Logik bleiben unveraendert und sind fuer Betreiber weiter per Plattform/SSH erreichbar. `05-install-overview.sh` loescht ausgemusterte Seiten jetzt aktiv per REST (sonst bliebe die Seite auf Bestandsgateways per URL erreichbar). Pi 71 per `stromkreis-update --now` aktualisiert und verifiziert (Seite 404, Overview ohne Experten-Eintrag); uebrige Gateways ziehen das Paket naechtens.
- **SD-Karten-Image wird nach der Einrichtung geloescht:** meldet ein Gateway Phase `fertig`, raeumt `/api/gateway/provision/v1/result` das Image samt Metadaten weg (`deleteImage`). Bestehende Images (Anlagen 50/51/66/71/74, je 1,7 GB) liegen noch am Server, keine davon ist `fertig`.

## Neu am 30.8.: App-Einrichtung per QR-Code/Einmal-Link (Tab auf der Anlagen-Detailseite)

Gegenstueck zum Onboarding der Stromkreis-App (Fork der openHAB-App in `~/Workspace/stromkreis-openhab-ios`, Serverkontrakt in deren `docs/stromkreis-onboarding.md`):

- **Neuer Tab "App-Einrichtung"** auf `/intern/anlagen/[id]`: erzeugt einen Einmal-Code (`?/app_code_erzeugen`), zeigt QR-Code (SVG, neue Dependency `qrcode`) und Link `https://stromkreis.net/app/setup/<token>` genau einmal an (7 Tage gueltig, ersetzt alte Codes; danach nur noch Metadaten "erstellt am/gueltig bis" aus dem Load).
- **Tabelle `app_setup_token`** (Migration `20260830190000`, nach dem `login_token`-Muster: nur SHA-256-Hash, `used_at`, Komposit-FK auf `battery_site`), `schema.sql` regeneriert.
- **`POST /api/app/setup/v1`** (unauthentifiziert, Token ist der Nachweis): loest den Code einmalig gegen `{cloudUrl, username, password, siteName}` ein (Cloud-Zugang der Anlage, Passwort aus `cloud_password` entschluesselt). Fehler als `{error}` mit deutschem Text (410 ungueltig/verbraucht, 409 noch kein Cloud-Konto ohne den Code zu verbrauchen, 400 kaputte Anfrage); verbraucht wird erst nach erfolgreichem Entschluesseln.
- **Fallback-Seite `/app/setup/[token]`** (oeffentlich, prueft ohne zu verbrauchen): Schritte fuer Mitglieder im Browser, Knopf "In der App öffnen" (`stromkreis://setup?token=…&origin=…`), QR-Code fuer den Desktop-Fall.
- **`/.well-known/apple-app-site-association`** als Route (Team `6U7435AK45`, Bundle `net.stromkreis.app`): Universal Links auf `/app/setup/*` oeffnen die App direkt, sobald die Datei live und die App entsprechend signiert ist. Bis dahin funktioniert `stromkreis://` sofort.
- Logik in `platform/src/lib/server/app-setup.js`; getestet gegen Wegwerf-DB (Einloesung, Doppel-Einloesung 410, Seite danach ungueltig, Action mit Session), `npm run check` und `npm run build` sauber. Noch nicht deployt, nicht committet.

## Neu am 28.8. (Tag 2): Detailseite mit Tabs, Cloud-Passwort nur kopieren, Nachtbudget aus der Main UI, Updater-Fix

- **Anlagen-Detailseite in zwei Tabs**: "Übersicht" (Einrichtung, Wechselrichter-Zugang, Batterie, Leistung, Gateway samt Cloud-Konto) und "Fernwartung" (SSH-Konsole samt Tunnel-Info, openhab.log-Protokoll). Beide Tab-Inhalte bleiben gemountet (nur per CSS versteckt), damit eine offene SSH-Sitzung den Tab-Wechsel uebersteht.
- **Cloud-Passwort wird nicht mehr angezeigt**: statt Klartext ein Knopf "Passwort kopieren" (Clipboard-API, 2 s Bestaetigung "Kopiert"). Das Passwort geht weiterhin an den Browser (noetig fuers Kopieren), steht aber nicht mehr auf dem Bildschirm.
- **Nachtbudget aus der Main UI entfernt** (alle 5 Wechselrichter-Profile, `overview.yaml`): die beiden Listenzeilen ("Heute in der Gemeinschaft", "Heute Nacht") samt Erwaehnungen in den Footer-Texten. Item `Stromkreis_NACHTBUDGET`, Regel-Logik und `nachtbudget_kwh` im Status-Push bleiben unveraendert.
- **Updater-Bug behoben (Selbstueberschreibung)**: `stromkreis-update` ruft install.sh auf, das das laufende Skript per `install_file` in-place ueberschreibt; bash las danach am alten Offset weiter und brach mit Syntaxfehler ab (Update selbst war da schon durch). Der Skriptrumpf steckt jetzt in `main()` und wird komplett eingelesen, bevor etwas laeuft.
- **pi-ssh.sh kann jetzt sudo**: Befehlsmodus erzwingt ein TTY (`-tt`) und beantwortet `[sudo] password`-Abfragen mit dem Anlagen-Passwort.
- Verteilung: neue Gateways bekommen die UI beim Setup; bestehende ziehen das Paket naechtens (03-05 Uhr, Pruefsummen-Abgleich) per `stromkreis-update`. Pi 71 wurde von Hand aktualisiert (`sudo stromkreis-update --now`) und ist verifiziert Nachtbudget-frei (JSONDB der Main UI: 0 Treffer). Der plattformseitige "Paket aktualisieren"-Knopf (Antwortfeld `update:true` im Status-Push) ist gateway-seitig vorbereitet, aber noch nicht gebaut.

## Neu am 28.8. (nachts): SSH-Konsole auf der Anlagen-Detailseite

- **Echtes SSH aus dem Browser** auf jedes Gateway mit stehendem Tunnel: auf der Anlagen-Detailseite unter Fernwartung "SSH-Konsole öffnen" (Knopf erscheint, sobald das Gateway seinen Tunnel-Schluessel gemeldet hat). Im Browser laeuft xterm.js; die eigentliche SSH-Verbindung (Protokoll, Passwort-Anmeldung als openhabian mit dem Anlagen-Passwort aus status.linux_password) haelt die Plattform serverseitig - das Passwort verlaesst den Server nie.
- **Weg ins Wartungsnetz:** das existiert nur im Namensraum des WireGuard-Containers; dort laeuft jetzt zusaetzlich **microsocks** (SOCKS5, Port 1080, nirgends veroeffentlicht, nur im Compose-Netz erreichbar). Alpine 3.20 hat kein microsocks-Paket, das Dockerfile baut es aus den Quellen (gepinnt v1.0.5, statisch). Die Plattform (`gateway-ssh.js`) verbindet sich mit eigenem Mini-SOCKS5-Client (keine Extra-Abhaengigkeit) durch den Durchgang zu `<wg_address>:22` und spricht darueber ssh2 (npm, einzige neue Laufzeit-Abhaengigkeit). Ohne Host-Key-Pruefung (wie accept-new bei wg-ssh.sh) - Restrisiko dokumentiert im Modulkopf.
- **Transport Browser<->Plattform:** SSE fuer die Ausgabe (Base64-Chunks, Puffer 256 KB fuer Reconnect), POST fuer Eingabe und Terminalgroesse (Eingaben gebuendelt ~15 ms), kein WebSocket noetig (adapter-node). Endpunkte unter `/intern/anlagen/[id]/ssh{,/stream,/input}`, alle nur angemeldet und mandanten-/anlagengebunden; Sitzungen (max. 20, Leerlauf-Timeout 30 min) leben im Plattform-Prozess.
- **Verifiziert lokal:** kompletter Roundtrip durch echten microsocks auf echten sshd (Login, `echo` ueber SSE zurueck), 401 ohne Login, 404 fuer fremde Anlage/Sitzung, klare Fehlertexte (keine Tunnel-IP, kein Passwort, Gateway nicht erreichbar mit 15-s-Deadline statt Minuten-Haenger). `npm run check` 395 Dateien, 0 Fehler; Produktions-Build ok (ssh2 extern, xterm gebundelt).
- **Compose:** Plattform bekommt `WG_SOCKS_HOST=wireguard`/`WG_SOCKS_PORT=1080`; echter Test auf dem Pi steht noch aus (braucht die Router-Weiterleitung UDP 51820).

## Neu am 28.8. (spaet): WireGuard-Fernwartung und Stromkreis-eigene openHAB-Cloud (beides als Container)

- **Entscheidung:** Fernwartung und Cloud kommen doch (Wunsch Martin), aber anders als bei ISCHLSTROM (WireGuard nativ auf s1) laeuft bei Stromkreis beides als Container im Compose-Stack.
- **WireGuard-Container** (`deploy/wireguard/`, Dienst `wireguard`, NET_ADMIN, UDP 51820 veroeffentlicht): baut `wg0` (10.88.0.1/24) auf, erzeugt sein Schluesselpaar beim ersten Start in das Volume `wg-keys` (die Plattform liest `server.pub` ueber dasselbe Volume, read-only) und gleicht die Peers jede Minute mit `GET /api/gateway/sync/wireguard-peers` ab (Bearer `GATEWAY_SYNC_TOKEN`; geloeschte Anlagen verlieren ihren Peer). Die Tunnel-IP vergibt `consumeProvisionCode` aus dem plattformweiten Pool `WG_SUBNET_PREFIX`.11-254 (Spalte `battery_site.wg_address`, unique); der Pi meldet seinen Public-Key in Phase `tunnel` (result-Endpunkt, Spalte `wg_public_key`). SSH auf ein Gateway: `deploy/wg-ssh.sh <tunnel-ip>` (laeuft ueber `docker compose exec wireguard ssh`, Anmeldung als openhabian mit dem Anlagen-Passwort).
- **openHAB-Cloud im Stack** (Dienste `cloud-app` auf 127.0.0.1:4100, `cloud-mongodb` mongo:6 mit `./data/mongo`, `cloud-redis`): offizielle Images, eigenes `config.json.template` (`deploy/openhab-cloud/`) mit gcm-Dummy-Block (FCM_SENDER_ID=0, sonst erkennen die openHAB-Apps den Server nicht als Cloud - Falle aus `docs/openhab-cloud.md` des ISCHLSTROM-Repos), `PROXY_HOST=remote.hac.stromkreis.net` (ohne eigenen Proxy-Host antwortet die ganze Website nur 401), NODE_ENV=production, Registrierung aus (Konten entstehen nur automatisch). Kein SMTP: Passwort-Reset laeuft ueber die Plattform ("Neues Cloud-Passwort" auf der Anlagen-Detailseite, Zustand `reset`).
- **Konten-Sync** (Dienst `cloud-sync`, gleiches Cloud-Image, `deploy/openhab-cloud/cloud-sync.{sh,js}`; Port von `scripts/ibm-provision/cloud-makeuser.js`): holt jede Minute die offenen Konten von der Plattform (`GET /api/gateway/sync/cloud-accounts`, `pending`/`reset`/`delete`), legt sie mit den Modellen des Cloud-Images an (User + Openhab uuid/secret, verifiedEmail=true) und meldet das Ergebnis (`POST /api/gateway/sync/cloud-result` -> `created`/`error`). Wichtig: das Cloud-Image rendert config.json erst im CMD (`run-app.sh`), nicht im Entrypoint - der Wrapper `cloud-sync.sh` rendert deshalb selbst.
- **Provisionierung erweitert** (Migration `battery_site_wireguard_cloud`, `schema.sql` regeneriert): je Anlage einmalig Tunnel-IP, `cloud_uuid`, `cloud_secret`/`cloud_password` (verschluesselt mit TOKEN_SECRET, `secrets.js`), Konto `anlage-<id>@stromkreis.net`, handytaugliches Passwort (9 Kleinbuchstaben + 3 Ziffern, iOS-App scheitert an Sonderzeichen). Antwort traegt INSTALL_WIREGUARD/WG_* und INSTALL_CLOUD/CLOUD_* - beides gated: ohne `server.pub` im Volume bzw. ohne `CLOUD_BASE_URL` in der .env bleiben die Teile am Gateway aus. Phasen `tunnel` und `cloud` wieder in SETUP_PHASES.
- **Gateway-Paket**: `07-openhab-cloud.sh` (UUID/Secret von der Plattform nach userdata schreiben, vor dem Addon-Schritt) und `08-install-wireguard.sh` (Schluesselpaar erzeugen, wg0 mit PersistentKeepalive 25, Public-Key melden, bis 3 min auf Handshake warten) sind zurueckportiert; `install-gateway.sh` hat wieder 12 Schritte, `02` traegt das openhabcloud-Addon samt baseURL ein, Verify prueft Secret und Handshake, Purge baut den Tunnel zurueck (Cloud-Identitaet bleibt). Anlagen-Detailseite zeigt Fernwartung (Tunnel-IP, Schluessel gemeldet) und Cloud-Konto (Benutzer, Passwort entschluesselt, Zustand, "Neues Cloud-Passwort").
- **Server eingerichtet** (28.8.): `.env` um `GATEWAY_SYNC_TOKEN`, `EXPRESS_KEY`, `WG_ENDPOINT`, `CLOUD_BASE_URL/DOMAIN/PROXY_HOST` ergaenzt (Backup liegt daneben), Caddy-Vhosts `hac.stromkreis.net` und `remote.hac.stromkreis.net` -> 127.0.0.1:4100 (validiert, neu geladen). Kernelmodul wireguard ist am Host vorhanden (5.15). **Noch offen (Martin):** DNS-A-Records fuer `hac.` und `remote.hac.stromkreis.net` und die Router-Weiterleitung UDP 51820 auf den Server - ohne sie bekommen hac.* keine Zertifikate bzw. kommt kein Tunnel-Handshake zustande; der Rest laeuft unabhaengig davon.

## Neu am 28.8.: Gateway-Paket portiert (openHAB-Setup, Steuerung, Plattform-Endpunkte)

- **Gateway-Paket `gateway/openhab/`**: vollstaendiger Port der ISCHLSTROM-Post-Install-Skripte (`Batteriemanagement/openhab/{setup,control,eeg-api,inverters}`), generisch fuer alle EEGs. "IBM" und alle Referenzen auf die ISCHLSTROM-Infrastruktur sind raus: Items heissen `Stromkreis_*` (Hauptschalter `Stromkreis_Aktiv`), Log-Prefix `[Stromkreis]`, Konfiguration `/etc/stromkreis/gateway.conf`, Installation `/opt/stromkreis/openhab`, Timer `stromkreis-update`, Dateien `stromkreis.items`/`stromkreis_*.js`, Thing-UIDs `*:stromkreis:*`. Entfallen (Architekturregel "nur outbound HTTPS, kein Zugriff in Heimnetze"): WireGuard-Fernwartung (08), eigene openHAB-Cloud (07), der interaktive Assistent (00-wizard) und prepare-sd (Stromkreis gibt nur fertige Images aus). Ablauf sonst identisch: 00-provision (Profil-Erkennung per Netzsuche, Phase `wechselrichter_unklar` mit Profilwahl von der Plattform), Passwoerter, Addons, Preflight, Things per REST (02b, inkl. Warteschleife `wartet_auf_passwort`/`wartet_auf_wechselrichter`, Exit 75 = Wiederholung durch stromkreis-firstboot), Items+Persistence, Regeln (Steuerungskern `control/core.js` + Profil-Adapter), Overview-Seiten, Selbst-Update, Verify; `purge-gateway.sh` fuer erneute Tests. Profilverzeichnis `fronius` heisst jetzt `fronius-symo` (wie der Plattform-Profilschluessel); `gateway/profiles/` bleibt reine Hardware-Doku.
- **Bootstrap `platform/static/gateway/install.sh`** laedt jetzt wirklich das Paket: Code einloesen (wie gehabt), `stromkreis-gateway.tgz` + SHA-256 pruefen, nach `/opt/stromkreis` entpacken (Backup der Vorversion), `install-gateway.sh` starten. `gateway/openhab/setup/build-dist.sh` baut das Paket nach `platform/static/gateway/` (tgz, .sha256, VERSION; Overview-Seiten aus overview.yaml, Build-Artefakte in `.gitignore`); `deploy/deploy.sh` ruft es vor dem rsync auf.
- **Neue Gateway-Endpunkte der Plattform** (alle POST mit Anlagen-Token, mandantengebunden; `lib/server/gateway-data.js`): `/api/gateway/status/v1` (Status-Push, lean/full-Merge nach ISCHLSTROM-Vorbild, mappt `log_entries` auf `status.logs` und Versionsstaende flach in den Status; `linux_password`/WLAN ueberleben volle Meldungen), `/api/gateway/ladefenster/v1` (Community-Fenster aus `forecast_value` plus individualisiertes Sperr-Ende aus gemeldeter Kapazitaet/Ladeleistung/SoC, Ladefaktoren, Entladestart; Port von forecast.ts), `/api/gateway/crossover/v1` (mittlere Crossover-Zeiten der letzten bis zu 7 vollstaendigen Messtage aus `measurement`+`meter_code.kind` statt der ISCHLSTROM-Wochenview), `/api/gateway/wolken/v1` (Mittag-Fenster + Stundenwerte aus `weather`), `/api/gateway/provision/v1/secret` (einmalige Auslieferung der Wechselrichter-Zugangsdaten, danach geloescht). Migration `battery_site_inverter_secret` (`inverter_username`, `inverter_secret`), `schema.sql` regeneriert (dbmate-Docker). Provisionierungsantwort erweitert um `OH_ADMIN_USER`/`OH_ADMIN_PASSWORD` (= Linux-Passwort, fuer das Karaf-Admin-Konto) und `STROMKREIS_VORNAME` (Begruessung der Overview-Seite); Phasenliste um `wartet_auf_passwort`, `wartet_auf_wechselrichter`, `updater` ergaenzt, `tunnel` entfernt.
- **Anlagen-Detailseite**: neuer Abschnitt "Zugang zum Wechselrichter" (Benutzer/Passwort, Hinweis bei Phase `wartet_auf_passwort`; Passwort liegt bis zur Abholung in `battery_site.inverter_secret`, danach nur noch am Gateway).
- **Test auf dem Raspberry Pi**: Schritt-fuer-Schritt-Anleitung in `gateway/README.md` (Anlage anlegen mit gesetztem Profil, Image bauen/flashen, Phasen beobachten; ohne Wechselrichter endet der Test gewollt in "Wartet, wird automatisch fortgesetzt"). `npm run check` gruen (371 Dateien), Paket baut (192 KB), Migration gegen Wegwerf-Postgres 17 getestet.
- Offen: "Paket aktualisieren"-Knopf am Dashboard (der Updater pollt bislang nur naechtlich die Pruefsumme), Statushistorie je Anlage (ISCHLSTROM `openhabstatushistory`), Verschluesselung von `linux_password`/`inverter_secret` vor echten Anlagen.

## Neu am 26.8. (abends): Anmelden mit EEGFaktura, Hintergrund-Import, Anlagen-Einrichtung nach ISCHLSTROM-Modell

- **Login auf der Startseite ("Anmelden mit EEGFaktura")**: OIDC Authorization Code + PKCE gegen die EEG-Faktura-Keycloak (Realm `EEGFaktura`), neuer oeffentlicher Client `net.stromkreis.platform` (angelegt per `deploy/eegfaktura-local/kc-client.sh`; Mapper `tenant` und `access_groups` wie der app-Client, kein Audience-Mapper, weil das Go-Backend ein `aud`-Array nicht parsen kann). Token-Pruefung ohne Bibliothek gegen das JWKS (`platform/src/lib/server/oidc.js`); `sub` kommt aus dem ID-Token (Keycloak 26 laesst es im Access-Token weg). Je RC-Nummer im `tenant`-Claim wird ein Mandant automatisch angelegt (Slug = RC klein, Name und Gemeinschafts-ID aus `GET /api/eeg`, Standort aus einer kleinen Ortstabelle, sonst Bad Ischl), dazu `eegfaktura_source` (`auth_mode oidc`), eine Betreiber-`member`-Zeile (Schluessel `oidc_sub` bzw. E-Mail) und ein Import-Auftrag. Eine EEG: direkt `/intern`; mehrere: Auswahlseite `/anmelden/eeg` (signiertes Kurzzeit-Cookie), spaeter `/intern/eeg-wechsel`. Verifiziert per curl-Ablauf: `importer` (TE100200 + TE100300, Auswahl), `DerKassier` (nur TE100400, direkt).
- **Hintergrund-Import**: Login fordert `offline_access`, das Refresh-Token liegt AES-256-GCM-verschluesselt in `eegfaktura_oidc_token` (Schluessel `TOKEN_SECRET` in der Server-`.env`, Plattform und Worker teilen ihn). Neuer Compose-Dienst `worker` (`pipeline/Dockerfile`, `python -m stromkreis_pipeline worker`) arbeitet `eegfaktura_sync_job` ab: Phase `masterdata` (`GET /api/participant` -> `member` mit Rolle member, `participant_number`, `address`, `eegfaktura_participant_id`; `measurement_point` mit `member_id`), dann `energy` in 30-Tage-Stuecken mit 20 s Pause (`WORKER_PACE_SECONDS`) ueber die Bearer-Routen `/energystore/eeg/v2/{ecid}/meta` und `/raw` (ProtectApi nimmt nur Basic). Fortschritt in `job.progress`, Anzeige `SyncStatus.svelte` im Dashboard (alle 10 s neu geladen). Ein Auftrag nach dem anderen; verwaiste Auftraege (Heartbeat > 30 min) werden neu aufgenommen. Migration `oidc_login_sync`; `schema.sql` regeneriert.
- **Anlagen-Einrichtung nach ISCHLSTROM-Modell** (Migration `battery_site_provisioning`): Anlage bekommt Einrichtungscode `XXXX-XXXX` (60 Tage, `provision_code`), die SD-Karte traegt nur `openhabian.conf` (Hostname, Linux-Passwort, WLAN, Zeitzone) und `stromkreis-provision.conf` (Code + Plattform-URL). **Entscheidung 26.8. (spaet): Stromkreis gibt ausschliesslich fertige SD-Karten-Images aus**, keinen Zip-Download der Karten-Dateien (Zip-Route und Zip-Writer wieder entfernt; die Renderer in `gateway-provision.js` bleiben fuer den Image-Builder). Naechster Schritt: Image-Builder nach ISCHLSTROM-Vorbild (`website/src/lib/server/ibmImage.js`: openHABian-Release laden, Boot-Partition per `mcopy` befuellen, gzip; Image-Volume am Server, ein Build gleichzeitig, Loeschung nach `fertig`). Gateway-API: `POST /api/gateway/provision/v1 {code}` liefert Konfiguration samt frisch erzeugtem Anlagen-Token (nur Hash gespeichert; Code gilt bis `fertig` oder Ablauf), `POST /api/gateway/provision/v1/result {token, phase, message, inverter_type, hostname}` meldet Phasen (`gateway-provision.js`, Phasen-Vokabular aus ISCHLSTROM `setupPhases.js`). Assistent `SetupWizard.svelte` umgebaut: Material -> Registrieren (Mitglied mit Zaehlpunkt aus dem Import, WLAN optional) -> SD-Karte (Image, Builder folgt) -> Verbinden (Fortschritt, 15-s-Reload; Demo-Mandant weiterhin simuliert) -> Fertig. Detailseite zeigt Einrichtungsstand, Code, "Neuer Code". Ende-zu-Ende per curl getestet (Anlage 66 in te100300). Restrisiko: Linux-Passwort und WLAN-Passwort liegen im Klartext in `battery_site.status` (wie ISCHLSTROM vor der Verschluesselung); vor echten Anlagen verschluesseln (`secrets.js` ist da).
- **Anlage direkt im Tab "Anlagen" anlegen** (`NewSiteForm.svelte`): leerer Zustand nach dem ersten Import bzw. Button "Neue Anlage für ein Mitglied". Nur zwei Felder: Mitglied aus dem EEGFaktura-Import (Nummer, Name, Adresse, Zaehlpunkte; nur Rolle member) und Wechselrichtertyp; die Aktion `anlegen` setzt Name ("Anlage <Mitglied>"), Adresse des Mitglieds, Mandanten-Koordinaten, den Erzeugungs-Zaehlpunkt des Mitglieds und 10 kWh als Vorgabe. Danach Code und Link zur Anlage (Image-Download folgt mit dem Builder); Anlagen-Karten zeigen vor dem ersten Push den Einrichtungsstand samt Code. Getestet in te100300 (Anlage 67) und im leeren Mandanten te100400.
- Umgebungsvariablen neu (Server-`.env`, `platform/.env.example`): `OIDC_ISSUER`, `OIDC_CLIENT_ID`, `EEGFAKTURA_BASE_URL`, `TOKEN_SECRET`, `WORKER_PACE_SECONDS`; `PUBLIC_ORIGIN` liest die Plattform jetzt selbst (`$env/dynamic/public`). Ohne OIDC-Variablen bleibt nur der Einladungslink-Login.
- Pipeline: `RefreshTokenAuth`, `bearer_routes=True` im Client, `normalize_participants`, `load.upsert_members`/`link_points_to_members`, `worker.py`; Abhaengigkeit `cryptography`. Tests: 24 gruen (`tests/test_oidc_worker.py` neu; der seit 24.8. kaputte `TenantSource`-Test repariert). Plattform: `npm run build` und `npm run check` (0 Fehler) mit Node 22 (`~/.nvm/versions/node/v22.22.1`; das System-Node 20 ist fuer Vite 8 zu alt).
- Nicht committet (Martin macht git): alles unter `platform/src/lib/server/{oidc,secrets,eegfaktura-login,gateway-provision}.js`, Routen `auth/eegfaktura/*`, `anmelden/{eeg,fehler}`, `intern/eeg-wechsel`, `api/gateway/provision/v1/*`, `SyncStatus.svelte`, `NewSiteForm.svelte`, `SetupWizard.svelte`, Migrationen `20260826200000`/`20260826200100` samt `schema.sql`, `pipeline/{Dockerfile,stromkreis_pipeline/{secrets,worker}.py}` und Aenderungen in client/config/normalize/load/sync/cli, `deploy/docker-compose.yml`, `deploy/deploy.sh`, `deploy/eegfaktura-local/kc-client.sh`, diese Datei.

## Neu am 27.8. (spät): Wetterimport, Prognosemodell und Tab "Prognose" (Port von ISCHLSTROM eeg_forecast.py)

- **Wetterimport je Mandant** `pipeline/stromkreis_pipeline/weather.py`: Open-Meteo für den Standort `tenant.latitude/longitude` in die bestehende Tabelle `weather` (PK tenant_id + time). Erstbefüllung aus dem ERA5-Archiv ab dem ersten Messwert (180-Tage-Stücke), danach je Lauf der Forecast-Endpunkt (`past_days=92` überschreibt Prognosestunden mit eingetretenem Wetter, 16 Tage voraus); Stunden ohne Pflichtwerte werden übersprungen.
- **Prognosemodell** `pipeline/stromkreis_pipeline/forecast.py`: Portierung von `notebooks/forecast/eeg_forecast.py` (gbt-1.1). Drei `HistGradientBoostingRegressor` je Mandant (Verbrauch, Erzeugung, Eigendeckung je aktivem Zählpunkt und 15 min, plus q10/q90), rein exogen (Kalender samt Feiertagen/Schulferien, Sonnenstand NOAA, Wetter interpoliert auf 15 min); Überschuss abgeleitet, Energiebilanz geschlossen, Halbwertszeit-Gewichtung 120 Tage plus Niveau-Rekalibrierung (14 Tage, out of sample). Zielgrößen über `meter_code.kind` statt Beschreibungstexten; Teillieferungs-Gate `FORECAST_MIN_REPORTING_SHARE` 0.85 / `FORECAST_MIN_POINTS` 5 (kleine EEGs) / `FORECAST_MIN_TRAIN_DAYS` 14. Neue Abhängigkeiten `numpy`, `scikit-learn`.
- **Versionierte Läufe**: Migration `20260827100000_forecast.sql` legt `forecast_run`/`forecast_value` an (composite FK über tenant_id, nie überschrieben, Vergleich Prognose vs. Messung möglich). Die Prognose beginnt am Tag nach dem letzten vollständigen Messtag und füllt die Lücke bis heute plus 16 Tage.
- **Auslösung im Worker** (`worker.py`): direkt nach jedem Energie-Import (`refresh_forecast`, Ergebnis in `job.progress.forecast`, Fehler lassen den Import nicht scheitern) und periodisch bei leerer Auftragsschlange (`maintenance`: Wetter alle `WEATHER_REFRESH_HOURS` 6 h für alle Mandanten, neuer Lauf wenn älter als `FORECAST_MAX_AGE_HOURS` 24 h). CLI: `weather-import` und `forecast` (je `--tenant`).
- **Tab "Prognose"** im Dashboard `/intern?tab=prognose`: `lib/server/prognose.js` liest den jüngsten Lauf (Stundenreihe fürs Diagramm, Tagessummen für die Kennzahlen, Fenster heute bis heute plus 14 Tage) plus die Wetterstunden im selben Fenster; `PrognoseTab.svelte` mit Kennzahlen (Ø Verbrauch/Erzeugung/Eigendeckung/Deckungsgrad), SVG-Liniendiagramm (Verbrauch, Erzeugung, Eigendeckung als Fläche, p10-p90-Band) und darunter der Chart "Wetterentwicklung" (Globalstrahlung als Fläche mit Skala rechts, Temperatur als Linie mit Skala links, Regenstunden als Balken; die Tagestabelle "Prognose je Tag" ist am 28.8. dafür entfallen). Randtage mit unter 90 Intervallen fallen aus den Tagesmitteln.
- **Demo-Seed** füllt jetzt auch `measurement_daily` (`refresh_measurement_daily` nach dem Einfügen), sonst hätte der Demo-Mandant keine vollständigen Tage für Energie-Tab und Prognose.
- Der EEGFaktura-Importstatus auf `/intern` ist am 28.8. entschärft: die volle Statuskarte über den Tabs erscheint nur noch während eines laufenden oder fehlgeschlagenen Imports; ein abgeschlossener Import steht als dezente einzeilige Statusinfo am Seitenende (Zeitpunkt, Mitglieder/Zählpunkte, Datenbestand). Der Fokus oben liegt damit auf den Anlagen.
- Lokal verifiziert (Wegwerf-DB): Migration, Seed, echter Open-Meteo-Import (2808 h Bad Ischl), Prognoselauf in ~19 s (1449 Intervalle), Bilanz plausibel (nachts 0 Erzeugung, Mittagsspitze); `pytest` 38/38 grün (neu `tests/test_forecast.py`), `npm run check` grün. Offen: Genauigkeitsvergleich Prognose vs. Messung (View wie `energy_forecast_vs_actual`) und Anzeige der Prognosegüte im Tab.

## Neu am 27.8.: SD-Karten-Image-Bau auf der Plattform (Port von ISCHLSTROM ibmImage.js)

- **Image-Builder** `platform/src/lib/server/gateway-image.js`: laedt das aktuelle openHABian-64-bit-Release (GitHub-API, Cache unter `base/`, Netz-Fallback auf den Cache), entpackt per `xz`, spielt `openhabian.conf`, `stromkreis-provision.conf` und `user-data` per `mcopy` (mtools, ohne Root) auf die FAT-Boot-Partition und legt `site-<id>.img.gz` ab (gzip Level 6, ~1,3 GB). Ein Bau gleichzeitig ueber alle Mandanten, ~7 GB Platzbedarf geprueft, Metadaten `site-<id>.json` (Code, Stand), Fehler in `site-<id>.error`; `stale`, wenn der Code seit dem Bau erneuert wurde oder abgelaufen ist. Ablage `IMAGE_DIR` (Compose: Bind-Mount `./data/images:/var/lib/stromkreis/images`, deploy.sh legt das Verzeichnis an); Dockerfile installiert `xz` und `mtools`.
- **Zero-Touch-Autostart** wie bei ISCHLSTROM per cloud-init: `user-data` (gerendert in `gateway-provision.js` aus `platform/src/lib/server/gateway-firstboot/stromkreis-firstboot.{sh,service}`, eingebunden per Vite `?raw`) installiert die Unit `stromkreis-firstboot`; sie wartet die openHABian-Erstinstallation ab und laedt `https://stromkreis.net/gateway/install.sh` (`platform/static/gateway/install.sh`). Das Bootstrap-install.sh tauscht den Code gegen Konfiguration samt Token (`/etc/stromkreis/gateway.conf`, chmod 600), meldet Phase `konfiguration` und endet mit Exit 75 (`unvollstaendig`, alle 10 min neuer Versuch), solange es kein Gateway-Paket `/gateway/<profil>.tgz` gibt; sobald die Profile portiert sind, setzt die Einrichtung automatisch fort. `renderReadme` (Zip-Ablauf) entfernt, Stromkreis gibt nur Images aus.
- **UI**: gemeinsame Komponente `SdImage.svelte` (Bau anstossen, Fortschrittsphase mit 5-s-Reload, Download, stale-/Fehler-Hinweis) im Assistenten-Schritt "SD-Karte" und auf der Anlagen-Detailseite; Aktion `image_bauen` in `/intern` (site_id) und auf der Detailseite, Download tenant-gescoped unter `/intern/anlagen/<id>/image.img.gz` (Dateiname `stromkreis-<slug>.img.gz`). Anlagen ohne `linux_password` im Status (vor der Provisionierung angelegt) bekommen beim ersten Bau eines gesetzt.
- **Verbindungszustand "In Einrichtung"**: Anlagen ohne ersten Status-Push (`last_seen_at` null) zeigen statt rotem "Offline" ein gelbes "In Einrichtung" (Badge auf Karten, Detailseite und als Marker-Farbe auf der Standortkarte samt Legende; `connectionState()` in `site-format.js`). Die Detailseite sagt dann "Noch kein Status-Push: Die Anlage meldet sich, sobald der Raspberry Pi mit der SD-Karte startet" statt "noch nie gemeldet", und die Warnbox "Werte koennen veraltet sein" erscheint nur noch, wenn es je einen Push gab.
- Stolperstein beim Testen: `battery_site.id` kommt aus postgres.js als String (bigint); `getImageStatus`/`startImageBuild` normalisieren auf Number, sonst galt der eigene Bau als "anderes Image". Ende-zu-Ende am Server verifiziert: Bau Anlage 50 und 74 (~1,85 GB `site-<id>.img.gz`, Basis-Image-Cache ~1,2 GB), Download-Header ok (`stromkreis-<slug>.img.gz`), Fortschrittsanzeige und "anderes Image"-Hinweis korrekt.
- **Tab "Neue Anlage" entfernt** (samt `SetupWizard.svelte`, Aktion `aktivieren` mit simulierter Erstverbindung und Demo-Helfern): Anlagen entstehen jetzt nur noch ueber "Neue Anlage fuer ein Mitglied" im Anlagen-Tab (`NewSiteForm.svelte`, waehlbar nur Mitglieder ohne Anlage); SD-Karten-Image und Fortschritt liegen auf der Anlagenseite. Tabs sind noch anlagen/standorte/energie, `?tab=einrichtung` faellt auf Anlagen zurueck.
- Offen: Loeschen der Image-Dateien beim Loeschen einer Anlage (`deleteImage` liegt bereit, es gibt noch keine Loesch-Aktion), Verschluesselung der Passwoerter in `battery_site.status`, echte Gateway-Pakete je Profil.

## Neu am 27.8.: mengenabhängige Import-Stücke und Lastschutz-Pausen

- Der Energie-Import teilt das Fenster nicht mehr fix in 30-Tage-Stücke, sondern mengenabhängig (`chunk_size()` in `pipeline/.../sync.py`): Ziel ~500.000 15-Minuten-Werte je Anfrage. Bis ~170 Zählpunkte bleibt es bei 30 Tagen (alle Bestandsmandanten unverändert), eine EEG mit 500 Mitgliedern (~700 Zählpunkte) bekommt 7-Tage-Stücke, Minimum 1 Tag. Damit bleiben Antwortgröße und Last je Anfrage an den energystore etwa konstant, egal wie groß die EEG ist.
- Pausen macht jetzt `sync_tenant` selbst (Worker: `pace_seconds=WORKER_PACE_SECONDS`, CLI-Läufe Default 5 s): zwischen zwei Stücken mindestens pace_seconds, bei langsamen Antworten die doppelte Antwortzeit der letzten Anfrage (Duty-Cycle höchstens ~1/3). Banner-Text im Dashboard heißt jetzt "Zuletzt aktualisiert: von bis".
- Rechenbeispiel 500 Mitglieder, 2 Jahre Erstimport: ~104 Stücke; bei z.B. 5 s Antwortzeit je Stück und 20 s Pause gut 45 Minuten Laufzeit, der energystore ist dabei nur je ~5 s am Stück beschäftigt.
- Tab "Energie" neu ausgerichtet auf das Speicherpotential: die reinen Energiezahlen (Verbrauch, Erzeugung, Eigendeckung) sehen Betreiber ohnehin in EEG-Faktura. Der Tab zeigt jetzt den PV-Überschuss der letzten 30 Tage: Ø Überschuss pro Tag, Ø Netzbezug pro Tag und "davon mit Speicher nutzbar" (Ø von min(Überschuss, Netzbezug) je Tag, ohne Speicherverluste) plus Tagesdiagramm (nutzbarer Anteil kräftig, Rest hell, gestrichelte Durchschnittslinie). Unvollständige Tage (Meldeanteil < 50%) bleiben aus den Durchschnitten draußen; Zeitraumwahl (30/90 Tage, 12/24 Monate) entfällt. `energie.js` entsprechend auf eine feste 30-Tage-Auswertung reduziert.
- Tab "Energie": Abschnitt "Mitglieder im Zeitraum" (Tabelle je Mitglied) wieder entfernt, samt zugehöriger Abfrage in `energie.js`; der Tab zeigt Kennzahlen, Verlauf und Datenbestand. Der Import-Banner (`SyncStatus.svelte`) zeigt zusätzlich zum zuletzt aktualisierten Fenster den Gesamtzeitraum der vorhandenen Energiedaten ("Datenbestand: von bis", min/max Tag aus `measurement_daily`).

## Neu am 26.8. (spät): Tab "Energie" auf Importdaten, Tagesaggregat

- Tab "Energie" (`platform/src/routes/intern/EnergieTab.svelte`, Auswertung in `src/lib/server/energie.js`) zeigt die importierten EEG-Faktura-Daten des Mandanten: Zeitraum 30/90 Tage (je Tag) oder 12/24 Monate (je Monat) per `?tab=energie&zeitraum=`; Kennzahlen Verbrauch (mit Netzbezug), gemeinschaftliche Erzeugung (mit Überschuss), Eigendeckung (Anteil der Erzeugung), Autarkie; Verlaufsdiagramm mit Markierung unvollständiger Tage (weniger als 50% der Verbrauchszählpunkte mit Verbrauch > 0, Teillieferungen); Tabelle je Mitglied (Verbrauch, aus EEG, Deckung, Erzeugung, Überschuss); Datenbestand (Zählpunkte, Zeitraum, letzter Import). Semantik wie ISCHLSTROM: gemeinschaftliche Erzeugung = `total_production`, Eigendeckung = `self_use` = Erzeugung - Überschuss.
- Tabs kommen jetzt aus der URL (`?tab=anlagen|standorte|energie|einrichtung`), die Energie-Auswertung wird nur für den Energie-Tab gerechnet; die alte 14-Tage-Grafik ist ersetzt.
- Migration `20260826220000_measurement_daily`: Tagesaggregat `measurement_daily` (kWh, Intervalle, Intervalle > 0 je Zählpunkt, Kategorie und lokalem Tag) plus DB-Funktion `refresh_measurement_daily(tenant, von, bis)`. Die Pipeline ruft sie nach jedem Import-Stück für dessen Zeitraum (plus ein Tag Rand) auf (`load.refresh_daily` in `sync.sync_tenant`), gilt für Worker und geplanten Lauf. Grund: Monats-/Jahresauswertungen auf den 15-Minuten-Rohdaten brauchten 3 bis 9 s je Abfrage. Bestehende Daten wurden am Server einmalig per `select refresh_measurement_daily(id, '2023-01-01', current_date)` je Mandant nachgerechnet.

## Neu am 26.8. (abends): echte Adressen für die Dummy-Mitglieder

Die Mitglieder aller drei Dummy-EEGs in der EEG-Faktura-Testinstanz haben jetzt echte Straßen und Hausnummern aus Bad Ischl, Bad Goisern und Ebensee (OpenStreetMap-Auszug `deploy/eegfaktura-local/eegs/adressen.json`, Zuweisung und API-Update per `set-addresses.py`, Wohn-, Rechnungs- und Zählpunktadressen). Die Plattform-Mitglieder (`member.address`) wurden über neu eingereihte Import-Aufträge nachgezogen. Namen bleiben erfunden.

Koordinaten der Mitglieder: Migration `20260826210000_member_coordinates` (`member.latitude/longitude/geocoded_address/geocoded_at`). Der Worker geokodiert nach jeder masterdata-Phase alle Mitglieder, deren Adresse sich seit der letzten Geokodierung geändert hat (Nominatim, strukturierte Anfrage mit Hausnummer, dann Freitext, nur AT, 1 Anfrage/s), und zieht Anlagen des Mitglieds nach, die noch die alte Mitgliedsadresse tragen (Adresse) bzw. auf den alten Mitgliedskoordinaten oder dem Gemeinschafts-Mittelpunkt liegen (Koordinaten); von Hand verschobene Anlagen bleiben unberührt. "Neue Anlage für ein Mitglied" nimmt jetzt die Mitgliedskoordinaten, Fallback bleibt der Gemeinschafts-Mittelpunkt. Ohne Treffer bleibt `latitude` leer, `geocoded_address` wird trotzdem gesetzt (kein Dauerversuch; erneut bei Adressänderung).

## Neu am 26.8.: Kontrolle des Testdeployments

- **EEG-Faktura-Testinstanz lag seit dem Server-Neustart am 24.8. (20:55, Kernel-Update) still**: alle zehn Container "Exited", weil weder Upstream-Compose noch Override eine Restart-Policy hatten. Behoben: `restart: unless-stopped` für alle Dienste in `compose.override.yaml` (am Server eingespielt), Stack per `up -d` gestartet. Daten unversehrt (Volumes), Smoke-Test grün: SPA, Admin-Portal, OIDC-Discovery (Issuer passt), Manager-Token, ProtectApi per Basic (`metadata` 7 Zählpunkte mit Daten, `masterdata` 7 Teilnehmer). Der Stromkreis-Stack hatte die Policy schon und war durchgehend erreichbar.
- **Plattform am Server war hinter dem Repo**: Migration `eegfaktura_community_id` vom 24.8. fehlte. `deploy/deploy.sh` gefahren, Migration angewendet, Stack neu gebaut.
- **Dummy-Energiedaten in der Test-EEG, lückenlos 21.5.2023 bis 25.8.2026** (anschließend an die Musterdaten; alle 11 Zählpunkte, 114.528 Viertelstunden, 166 MWh Erzeugung, 117 MWh Verbrauch, 42% Eigendeckung des Verbrauchs): neues Skript `deploy/eegfaktura-local/gen-energy-report.py` erzeugt den EDA-Report synthetisch (Profile, Sonnenstand, Wetter, exakte EEG-Identitäten), per energystore-`singleUpload` eingespielt; Format aus dem energystore-Quellcode (`excel/ExcelSourceNew.go`) abgeleitet. Damit hat der Importer eine realistische Gegenstelle für den Meldeanteil-Gate und die Dashboards. Die Musterdaten Jan bis Mai 2023 bleiben davor.
- **Zwei weitere Dummy-EEGs in der Testinstanz** (26.8. abends): `TE100300` EEG Sonnenstrom Bad Goisern (20 Mitglieder, 27 Zählpunkte, 7 PV mit 62 kWp; Benutzer `importer`, dessen `tenant`-Claim jetzt `["TE100200","TE100300"]` ist) und `TE100400` EEG Traunsee Ebensee (20 Mitglieder, 23 Zählpunkte, 3 PV mit 27 kWp; neuer Benutzer `DerKassier`, nur `TE100400`), je zwei Jahre Energiedaten 26.8.2024 bis 25.8.2026. Werkzeuge in `deploy/eegfaktura-local/`: `gen-members.py` (Mitglieder-Konfiguration `eegs/<RC>.json`), `gen-eeg.py` (Stammdaten- und Energiedaten-Excel aus der Konfiguration; ersetzt `gen-energy-report.py`), `kc-user.sh` (Keycloak-Benutzer und `tenant`-Attribut per Admin-API), `setup-eeg.sh` jetzt konfigurationsgesteuert. Verifiziert: Stammdaten-Import 204, energystore-Import ok, ProtectApi per Basic liefert je EEG alle Zählpunkte, Mandantentrennung greift (`importer` auf `TE100400`: 403).
- **Zugangsdaten zum Nachschlagen**: `ZUGANGSDATEN.md` im Repo-Wurzelverzeichnis (gitignored) mit allen Test-Logins (Salzkammerstrom-Login-Link vom 26.8., gültig bis 2.9.; EEG-Faktura-Benutzer, Client-Secrets, Keycloak-Admin).
- Nicht committet (Martin macht git): `.gitignore`, `deploy/eegfaktura-local/compose.override.yaml`, `deploy/eegfaktura-local/README.md`, `deploy/eegfaktura-local/{gen-members.py,gen-eeg.py,kc-user.sh,setup-eeg.sh,eegs/}`, diese Datei.

## Neu am 24.8.: EEG-Faktura-Testinstanz auf `server`

- Vollständiger EEG-Faktura-Stack läuft auf `server` unter `/home/martin/Container/eegfaktura/` (Upstream-Compose als Tarball plus `compose.override.yaml`, Realm-Patch, Setup-Skript aus `deploy/eegfaktura-local/`). Öffentlich per TLS hinter dem Server-Caddy: SPA https://eegfaktura-test.stromkreis.net, Admin-Portal https://admin.eegfaktura-test.stromkreis.net, Keycloak https://auth.eegfaktura-test.stromkreis.net (Konsole nur aus dem Heimnetz); drei A-Records in Route 53, Caddy-Block in `~/Container/caddy/Caddyfile` (Backup `Caddyfile.bak-eegfaktura`). Secrets nur in `server:.../secrets.env`. Test-EEG `TE100200` mit Muster-Stammdaten und Energiedaten Jan bis Mai 2023. Aufbau von Null ist reproduzierbar (`down -v`, `up -d`, `setup-eeg.sh`). Befunde und Fallstricke: `docs/eegfaktura-lokal.md`; Betrieb: `deploy/eegfaktura-local/README.md`.
- **Importer erstmals Ende-zu-Ende gelaufen** (gegen die Testinstanz, lokale Wegwerf-DB, Mandant `testeeg`): `eegfaktura-probe` ok, `eegfaktura-sync --full` 278.240 Zeilen, 11 Zählpunkte mit Namen, alle fünf Kategorien. Dabei behobene Importer-Fehler: `ecId` muss die Gemeinschafts-ID sein (neue Migration `eegfaktura_community_id`, Spalte `eegfaktura_source.community_id`, Client-Parameter `ec_id`; `schema.sql` regeneriert), Basic-Auth-Kodierung je Dienst (Backend Standard-, energystore URL-safe Base64), `masterdata`-Feldnamen (`firstname`/`lastname`/`meters`), Sommerzeit-Duplikate im energystore (Dedup in `normalize_rawdata`). 15 Tests grün.
- Noch nicht committet (Martin macht git): `docs/eegfaktura-lokal.md`, `docs/eegfaktura-api.md` (Fallstricke), diese Datei, `deploy/eegfaktura-local/*`, Migration `20260824190000_eegfaktura_community_id.sql` samt `schema.sql`, Pipeline-Änderungen in `client.py`, `config.py`, `cli.py`, `normalize.py` und Tests.
- **Für ISCHLSTROM** heißt das: in `eegfaktura_source` neben der RC-Nummer die Gemeinschafts-ID eintragen (steht in der EEG-Stammdatenansicht der SPA), sonst bleibt der Import leer.

## Entscheidungen

- **Salzkammerstrom ist Demo-Mandant, ISCHLSTROM erster echter Test-Mandant (8.8.).** Der Dummy Salzkammerstrom (Standort Bad Ischl, per Migration angelegt) bekommt erfundene, generierte Daten und dient zum Entwickeln und Vorführen; er braucht keinen EEG-Faktura-Zugang. ISCHLSTROM wird als Mandant angelegt, sobald der Importer steht, und testet die echte Kette mit echten EEG-Faktura-Daten (Zugangsdaten nur in der Server-`.env`). Die eigene ISCHLSTROM-Infrastruktur läuft parallel weiter; ein vollständiger Umzug bleibt eine spätere Option.
- **Migrationswerkzeug: dbmate.** Reines SQL unter `platform/db/migrations/`, das generierte `platform/db/schema.sql` ist eingecheckt und der Vertrag für die Pipeline. Referenzen zwischen Fachtabellen als zusammengesetzte Fremdschlüssel über `(tenant_id, id)`, damit mandantenübergreifende Verweise auf DB-Ebene scheitern.
- **Rollen:** `member` (Mitglied), `board` (Vorstand), `operator` (Betreiber). Jeder Mandant hat mindestens einen Betreiber-Login; das sichert der Onboarding-Ablauf, kein DB-Constraint. Betreiber brauchen eine E-Mail (Check-Constraint).
- **Login ohne SMTP:** Einmal-Login-Links (7 Tage, nur SHA-256-Hash gespeichert) werden per Admin-CLI erzeugt und manuell übergeben; sie werden gegen eine Session (30 Tage, HttpOnly-Cookie) eingetauscht. Der spätere E-Mail-Magic-Link nutzt denselben Token-Fluss, es fehlt nur der Versand.
- **Betreiber-Login mit EEGFaktura-Konto: seit 26.8. gegen die Testinstanz umgesetzt** (siehe oben; fuer eegfaktura.at fehlt weiterhin der vom VFEEG-Team registrierte Client). Urspruengliche Einschaetzung vom 8.8.: Wunschziel ist "Anmelden mit EEGFaktura" per OIDC gegen deren Keycloak (login.eegfaktura.at, Realm `EEGFaktura`). Geprüft: deren bestehender Client `at.ourproject.vfeeg.app` erlaubt weder Password Grant noch fremde Redirect-URIs; SSO braucht daher einen vom VFEEG-Team registrierten eigenen Client. Ein Anfrage-Entwurf liegt unter `docs/drafts/eegfaktura-sso-anfrage.md` (gitignored, noch nicht versendet). Bis dahin bleibt der Magic-Link-Fluss der Betreiber-Login; die Entwicklung hängt an nichts davon ab. OIDC mündet später in denselben Session-Fluss (Identität per E-Mail/`sub` auf eine `member`-Zeile mappen, dann `createSession`).

## Was läuft (produktiv erreichbar)

- **https://stromkreis.net**: deutsche Landingpage, `/anmelden/<token>`, geschützter Bereich `/intern`, Abmelden. DNS zeigt auf die Heim-IP, Caddy am Server terminiert TLS (`~/Container/caddy/Caddyfile`, Backup `Caddyfile.bak-stromkreis`).
- **Server-Stack** auf Host `server` unter `/home/martin/Container/stromkreis/`: `docker-compose.yml` (aus `deploy/` synchronisiert), `.env` (nur am Server: `POSTGRES_PASSWORD`, `PUBLIC_ORIGIN=https://stromkreis.net`), `src/` (rsync-Kopie des Repos), `data/postgres/`. Dienste: Postgres 17 (nur intern), Plattform auf 127.0.0.1:4000, dbmate-Migrate als One-Shot, `demo-heartbeat` (hält die Demo-Anlagen online, siehe unten).
- **Angewendete Migrationen** (lokal getestet und am Server eingespielt): `initial_schema` (tenant, member, measurement_point), `seed_salzkammerstrom`, `operator_role`, `auth` (login_token, session), `energy_series_weather` (8.8.: `meter_code` mit stabilem `kind`-Schlüssel total_consumption/production_share/self_use/total_production/overshoot, `measurement` als 15-Minuten-Reihen je Zählpunkt und Kategorie in kWh mit UTC-Intervallbeginn und zusammengesetztem PK, `weather` als Stundenwetter je Mandant mit Spaltensatz der ISCHLSTROM-Loader), `battery_site` (8.8.: openHABian-Anlagen mit Wechselrichterprofil, Token-Hash, `last_seen_at`, letztem Status-Push als jsonb), `battery_site_location` (8.8.: latitude/longitude je Anlage für die Standorte-Karte), `battery_site_address` (8.8.: Anschrift als Text). Zusammengesetzte FKs und Checks per Smoke-Test verifiziert; `schema.sql` per Container-`pg_dump` aktualisiert.
- **Dashboard unter `/intern`** (8.8.), drei Tabs: Standard-Tab "Anlagen" mit Karten je openHABian-Gateway (Betreiber sollen sofort sehen, dass alles läuft: Online/Offline ab 10 min ohne Push, SoC-Balken, PV/Batterie/Netz-Leistung mit Fronius-Vorzeichen, Versionen, Ladesperre-Hinweis); Tab "Standorte" mit Anlagenkarte (maplibre-gl + OpenFreeMap-Kacheln wie die ISCHLSTROM-Mitgliederkarte: EU-gehostet, kein API-Key, kein Tracking; Marker grün/rot nach Online-Status, Popup mit Name und Adresse, `SiteMap.svelte`; maplibre-gl auf ^4.7.1 gepinnt wie ISCHLSTROM, mit 6.x blieb die Basiskarte leer, nur Marker sichtbar); Tab "Energie" mit KPI-Karten für den letzten vollständigen Tag (Verbrauch, Erzeugung, Eigendeckung, Autarkie) und dem 14-Tage-Balkendiagramm (reines SVG, Erzeugung gestapelt als Eigendeckung + Überschuss). Detailansicht je Anlage unter `/intern/anlagen/<id>` (Batterie, Leistung inkl. Hausverbrauch, Gateway-Infos, Protokoll, Einzelkarte); erreichbar per Klick auf die Anlagen-Karte im Dashboard und auf den Karten-Marker (Hover zeigt die Popup-Vorschau). Das Protokoll zeigt die letzten openhab.log-Zeilen aus dem Status-Push (`status.logs`, beim Demo-Mandanten generiert). "SSH-Konsole öffnen" führt zu `/intern/anlagen/<id>/konsole`: beim Demo-Mandanten eine simulierte Web-Konsole (als solche gekennzeichnet; bei Offline-Anlagen simulierter Timeout). Eine echte Konsole braucht später einen Reverse-Tunnel vom Gateway, da Gateways nur outbound erreichbar sind. Anzeigehelfer geteilt in `site-format.js`. Queries durchgehend mandantengefiltert.
- **Demo-Daten für Salzkammerstrom** sind am Server eingespielt: `platform/scripts/demo-data.js seed [slug]` (deterministischer Seed, ersetzt Demo-Daten vollständig, Betreiber- und Vorstandskonten bleiben). Erzeugt 10 Mitglieder, 15 Zählpunkte, 35 Tage 15-Minuten-Reihen mit eingehaltenen EEG-Identitäten (Anteil = Gesamte Erzeugung; Eigendeckung + Überschuss = Anteil; Zuordnung proportional zum Verbrauch), Stundenwetter passend zur Tagesbewölkung und 16 Anlagen (14 online, 2 offline: Ebensee und Gosau) verteilt übers Salzkammergut von Gmunden bis Obertraun, von Fuschl bis Hallstatt (echte Ortskoordinaten, erfundene Adressen); druckt die Gateway-Tokens. Der Compose-Dienst `demo-heartbeat` simuliert alle 5 min die Status-Pushes (`demo-data.js heartbeat`), sonst fallen alle Demo-Anlagen 10 min nach dem Seed auf Offline; die zwei Offline-Anlagen bleiben dabei auf festem Alter (3 h bzw. 42 min).
- **Neu am 9.8., am Server seit dem Deploy vom 9.8.** (Migration `eegfaktura_source` ist eingespielt; der Importer selbst lief noch nie gegen die echte API): EEG-Faktura-Importer in `pipeline/` (`stromkreis_pipeline/eegfaktura/`: client mit beiden Auth-Wegen basic und client_credentials, Normalisierung auf das quellenneutrale Zwischenformat, COPY-Upsert, inkrementeller Sync mit 14 Tagen Überlappung in Monats-Chunks, CLI `eegfaktura-probe`/`eegfaktura-sync`, 15 Tests grün inkl. Wegwerf-Postgres) plus Migration `eegfaktura_source` (Zugangskonfiguration je Mandant ohne Secrets; `measurement.quality` für qov, Grundlage Meldeanteil-Gate). `schema.sql` regeneriert. Secrets-Konvention: `EEGFAKTURA_<SLUG>_USER/_PASSWORD` bzw. `_CLIENT_ID/_CLIENT_SECRET` in der Server-`.env`. Details `pipeline/README.md` und `docs/eegfaktura-api.md`.
- **Neu am 9.8., am Server seit dem Deploy vom 9.8.:** Vierter Dashboard-Tab "Neue Anlage" unter `/intern`: Einrichtungs-Assistent für eine neue Anlage von der leeren SD-Karte bis zum laufenden Batteriemanagement (`SetupWizard.svelte`; als simulierter Demo-Ablauf gekennzeichnet wie die SSH-Konsole). Fünf Schritte: Material-Checkliste (inkl. unterschriebener Risikoaufklärung), openHABian per Raspberry Pi Imager flashen (simulierter Schreibvorgang), Anlage registrieren (echtes Formular mit Mitglied, Profil, Adresse, Koordinaten, Kapazität, kWp; Server-Action `anlegen` legt die `battery_site` mandantengefiltert an, Token wird einmalig angezeigt und nur als Hash gespeichert, Anlage bleibt offline bis zum ersten Push), Gateway verbinden (`gateway.conf`-Vorlage mit Token, simulierte Erstverbindung; Server-Action `aktivieren` schreibt den ersten Status-Push samt Logs, danach ist die Anlage online und erscheint unter Anlagen/Standorte), Abschluss mit Link zur Detailansicht. `demo-data.js heartbeat` hält auch Assistent-Anlagen frisch (alle Anlagen außerhalb der SITES-Liste mit mindestens einem Push); `seed` ersetzt sie wie alle Demo-Anlagen. Nebenbei: `db.js` exportiert `sql` jetzt als Proxy, damit postgres.js-Helfer wie `sql.json` auch über den Lazy-Wrapper funktionieren; `site-format.js` kennt das Profil `fronius-snapinverter`. Ende-zu-Ende gegen Wegwerf-Postgres getestet (anlegen, aktivieren, heartbeat, Detailseite).
- **Betreiber-Account** Martin Aigner <martin@maigner.net> bei Salzkammerstrom existiert. Ein Einmal-Login-Link wurde am 7.8. erzeugt (gültig bis 14.8., falls noch nicht verwendet); neuer Link jederzeit per `invite` (siehe unten).

## Server-Verwaltung

```sh
ssh server   # geht von Workstation und MacBook (Key-only)
cd /home/martin/Container/stromkreis
docker compose exec platform node scripts/admin.js tenant:list
docker compose exec platform node scripts/admin.js tenant:create <slug> <name> <breite> <laenge>
docker compose exec platform node scripts/admin.js operator:create <tenant-slug> <name> <email>
docker compose exec platform node scripts/admin.js invite <tenant-slug> <email>
docker compose exec platform node scripts/demo-data.js seed [tenant-slug]   # Demo-Daten neu erzeugen
```

Sicherheit: Port 22 ist am Router offen; sshd erlaubt nur Public-Key-Login (`/etc/ssh/sshd_config.d/60-hardening.conf`), fail2ban ist aktiv (`/etc/fail2ban/jail.local`: 5 Fehlversuche in 10 min ergeben 1 h Sperre, eskalierend bis 1 Woche; Heimnetz 192.168.178.0/24 nie). Vorlagen der beiden Dateien liegen in `server:~/staging/`.

## Deployment

`deploy/deploy.sh` von der Entwicklungsmaschine aus: rsync des Arbeitsstands nach `server:.../src/`, Image-Build am Server, Migrationen, `docker compose up -d`. Kein git im Spiel; funktioniert vom MacBook, sobald das Repo dort liegt.

## Lokale Entwicklung

- `platform/`: `npm install`, dann `npm run dev` bzw. `npm run build && npm run check`. Runtime-Dependency: `postgres` (postgres.js, lazy verbunden).
- Lokale DB für Tests: Wegwerf-Container, z.B. `docker run -d --rm --name pg -e POSTGRES_PASSWORD=postgres -p 54329:5432 postgres:17-alpine`, dann `DATABASE_URL="postgres://postgres:postgres@127.0.0.1:54329/stromkreis_dev?sslmode=disable" npm run db:migrate`.
- `db/schema.sql` aktualisiert dbmate nur, wenn lokal `pg_dump` vorhanden ist (`postgresql-client`; am Mac `brew install libpq`). Sonst wie bisher per `pg_dump` im Container erzeugen (Schema-Dump plus `INSERT`-Zeilen von `schema_migrations`).

## Offene Punkte

1. **Heim-IP statisch oder dynamisch?** Ungeklärt. Bei dynamischer IP braucht stromkreis.net einen DynDNS-Mechanismus, sonst fallen Website und SSH-Zugang beim IP-Wechsel aus.
2. **SMTP-Anbieter** für Magic-Link-Versand ist nicht entschieden; bis dahin Links per Admin-CLI.
3. **Admin-CLI ist ungeschützt** gegenüber jedem mit Server-Shell; vor Onboarding externer EEGs überdenken.
4. **Nächster Roadmap-Schritt (Phase 1):** Der EEG-Faktura-Importer ist seit 9.8. gebaut (siehe oben), aber noch nie gegen die echte API gelaufen. Als Nächstes: ISCHLSTROM als Mandant anlegen, `eegfaktura-probe` mit den Portal-Zugangsdaten (auth_mode basic) fahren, dann Erstimport und Diff gegen einen bekannten Excel-Export (Phase C in `docs/eegfaktura-api.md`). Danach Excel-Fallback-Parser, Wetterimport und Prognose portieren.
5. **EEGFaktura-Anfrage ist raus (9.8.):** Ticket an das VFEEG-Team versendet mit Bitte um zwei Keycloak-Clients: `net.stromkreis.platform` (SSO-Login, Authorization Code + PKCE) und `net.stromkreis.import` (API-Import per client_credentials, zunächst RC101533). Versendete Fassung in `docs/drafts/eegfaktura-sso-anfrage.md`; auf Antwort warten. API-Erkenntnisse (Endpunkte, Auth-Wege, Fallstricke) und Importer-Plan in `docs/eegfaktura-api.md`; unabhängig davon sofort testbar ist der Basic-Weg gegen den metadata-Endpunkt (Achtung: Millisekunden, nicht Sekunden).
6. Git macht Martin weiterhin manuell; die Änderungen vom 8.8. (dieser Stand, `.gitignore`, `CLAUDE.md`, `architektur.md`, `roadmap.md`, Migrationen `energy_series_weather`, `battery_site`, `battery_site_location` und `battery_site_address` samt `schema.sql`, `scripts/demo-data.js`, Dashboard `/intern` inkl. `SiteMap.svelte`, neue Dependency `maplibre-gl` ^4.7.1, `deploy/docker-compose.yml` mit `demo-heartbeat`) sind noch nicht committet. Dazu kommen vom 9.8.: `docs/eegfaktura-api.md`, Migration `eegfaktura_source` samt regeneriertem `schema.sql`, das Importer-Modul `pipeline/stromkreis_pipeline/` mit Tests und `pipeline/README.md`/`pyproject.toml`, sowie der Einrichtungs-Assistent (`SetupWizard.svelte`, Änderungen an `/intern/+page.svelte`, `+page.server.js`, `site-format.js`, `lib/server/db.js`, `scripts/demo-data.js`).
