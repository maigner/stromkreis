# secrets & local config
.env
.env.*
!.env.example
.pg_service.conf
.pgpass

# python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# data dumps
*.dmp
*.csv
!**/fixtures/**/*.csv

# drafts (nicht veröffentlichen, z.B. Anfrage-Entwürfe)
docs/drafts/

# editors / os
.DS_Store

# lokale Zugangsdaten fuer die Test-EEGs (nie einchecken)
ZUGANGSDATEN.md

# Gateway-Paket (Build-Artefakte, erzeugt von gateway/openhab/setup/build-dist.sh)
platform/static/gateway/stromkreis-gateway.tgz
platform/static/gateway/stromkreis-gateway.tgz.sha256
platform/static/gateway/VERSION
